# Crawler UI

## Run the project

Execute the following bash commands:

`
    yarn upgrade
    yarn build --no-cache --dist-dir ./dist
    yarn install
    yarn build
    yarn global add serve
    serve -s dist -l 3124
`
